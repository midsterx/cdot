#include <stdio.h>

int main(int c , int d)
{
  a = 3 + 5*4 + 2;
  a = b * 3;
  c = d + 4;
  int var =3 , i = d;
  int a = c + d + 2;
  int b = f(a,b,c);
  // b = f(a,b,c);
  // f(a,b,c);

  if (x > y)
  {
	  u = 1;
  }
  fact = u;

  do
  {
    u = 2;
  } while (z > w);

  if (x > y)
  {
	  u = 1;
  }
}
